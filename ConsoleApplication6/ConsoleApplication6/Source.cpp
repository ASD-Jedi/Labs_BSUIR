#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <malloc.h>
#include <iostream>
using namespace std;

/*int main()
{
	char buffer[30];
	int l[30], n = -1, i, j = 0;
	for (i = 0; i < 30; i++)
		l[i] = 0;
	//scanf("%d", &n);
	//m = (char*)malloc(sizeof(char)*n);
	//l = (int*)malloc(sizeof(int)*n);
//	scanf("%c", &buffer);
	gets(buffer);
	for (i = 0; i < 30; i++)
	{
		while ((buffer[i] - '0') < 10)
		{
			l[j] = l[j] * 10 + (buffer[i] - '0');	
			i++;
			n = -1;
		}
		if (((buffer[i] - '0') >= 10) && (n == -1))
		{
			j++;
			n = 1;

		}

	}
	for (i = 0; i < 30; i++)
		printf("%5d", l[i]);

//	n = buffer - '0';
//	printf("%d", n);
}*/

int main()
{

}